<%- model_class = Archive -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<%= render 'table_archives',  model_class: model_class %>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_archive_path,
            :class => 'btn btn-primary' %>
